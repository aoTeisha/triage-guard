"""Real Prolog, Datalog and OPA engines behind small Python wrappers.
The graph and the monitor *propose*; these decide, and every refusal comes
with a reason. A wrapper that cannot reach its engine refuses — it
never falls back to a Python approximation.
"""
