# Langfuse — must match the values in langfuse/.env (LANGFUSE_INIT_PROJECT_*)
LANGFUSE_PUBLIC_KEY=pk-lf-11111111-1111-1111-1111-111111111111
LANGFUSE_SECRET_KEY=sk-lf-11111111-1111-1111-1111-111111111111
LANGFUSE_BASE_URL=http://localhost:3000

# crewAI builds each agent's LLM at load time, so this must be SET even in mock mode
# — it is never called, so any non-empty placeholder works until you go live.
# Swap in the real key from home assignment 2 before enabling crew.kickoff().
OPENROUTER_API_KEY=sk-or-v1-...
MODEL=openrouter/anthropic/claude-sonnet-4
