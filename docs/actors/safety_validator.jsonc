{
  // Deterministic verdict on the settled acuity. Never writes State.
  // Spec arrows 9 -> 10 (pass) / 10-fail (route to human gate).
  "actor": "Safety Validation Agent",
  "type": "Deterministic",
  "reads": "proposed classification",
  "proposes": "verdict (pass/fail)",
  "tech": "Prolog / Datalog / Z3 / OPA",
  "implementation": "app.flow.agents.invoke_safety_validation",
  "mock": "app/agents/mocks/safety_validator.json"
}
