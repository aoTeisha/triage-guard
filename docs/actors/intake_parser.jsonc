{
  // Deterministic webform validator. Proposes DATA_PARSED; never writes State.
  // Spec arrows 3 -> 4 (valid), 16 (missing fields), 17 (failed), 18 (invalid/injection).
  "actor": "Intake Parser Agent",
  "type": "Validator (deterministic)",
  "reads": "webform / structured intake (mock)",
  "proposes": "Data_Parsed proposal / field-presence check",
  "tech": "Deterministic schema validation",
  "implementation": "app.flow.agents.invoke_intake_parser",
  "mock": "app/agents/mocks/intake_parser.json"
}
